<!DOCTYPE html>
<html class="dark" lang="<%= lang %>">
  <%- include('partials/head') %>
  <link rel="stylesheet" href="/assets/css/index.css" />
  <body class="light">
 

    <div id="preloader">
      <div class="spinner"></div>
    </div>

    <div class="full-layout">
      <div class="main-layout">
        <div class="main-panel">
          <div class="matrix-container">
            <canvas id="matrix-canvas"></canvas>
          </div>

          <!-- 🔹 코드 프리뷰 섹션 -->
          <section class="welcome-section">
            <h1 class="welcome-title">🧪 HTML/CSS 코드 미리보기</h1>
            <p class="welcome-subtitle">코드를 입력하면 아래에 실시간으로 결과가 보여요!</p>
          </section>

          <section class="code-editor-section">
            <textarea id="code-input" placeholder="여기에 HTML과 CSS 코드를 입력하세요...">
<!-- 예시 -->
<style>
  body {
    background: #111;
    color: #fff;
    text-align: center;
    font-family: sans-serif;
  }
  h1 {
    color: #00f5d4;
    margin-top: 100px;
  }
</style>
<h1>Hello World!</h1>
            </textarea>
            <iframe id="preview-frame"></iframe>
          </section>

          <div class="spacer"></div>
        </div>
      </div>
    </div>

    <%- include('partials/scripts') %>

    <style>
      .welcome-section {
        padding: 60px 20px 30px;
        text-align: center;
      }

      .welcome-title {
        font-size: 2.2rem;
        margin-bottom: 10px;
      }

      .welcome-subtitle {
        font-size: 1.1rem;
        color: #aaa;
      }

      .code-editor-section {
        padding: 20px;
        display: flex;
        flex-direction: column;
        gap: 20px;
      }

      #code-input {
        width: 100%;
        height: 250px;
        font-family: monospace;
        font-size: 14px;
        padding: 12px;
        background: #1e1e1e;
        color: #00f5d4;
        border: 1px solid #555;
        border-radius: 10px;
        resize: vertical;
      }

      #preview-frame {
        width: 100%;
        height: 400px;
        border: 1px solid #444;
        background: white;
        border-radius: 10px;
      }
    </style>

    <script>
      const input = document.getElementById('code-input');
      const iframe = document.getElementById('preview-frame');

      function updatePreview() {
        const doc = iframe.contentDocument || iframe.contentWindow.document;
        doc.open();
        doc.write(input.value);
        doc.close();
      }

      input.addEventListener('input', updatePreview);
      window.addEventListener('DOMContentLoaded', updatePreview);
    </script>
  </body>
</html>
